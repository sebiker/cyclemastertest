{
  "name": "cyclemaster",
  "version": "1.0.0",
  "description": "WebSocket-based cycling measurement platform",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && node dist/index.js",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "app": "npm run build && node dist/index.js",
    "test": "NODE_OPTIONS=--no-deprecation playwright test",
    "test:ui": "NODE_OPTIONS=--no-deprecation playwright test --ui",
    "test:debug": "NODE_OPTIONS=--no-deprecation playwright test --debug"
  },
  "keywords": [
    "cycling",
    "websocket",
    "measurement"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0-alpha-2026-05-21",
    "@types/node": "^20.10.6",
    "@types/ws": "^8.5.10",
    "playwright": "^1.61.0-alpha-2026-05-21",
    "typescript": "^5.3.3"
  }
}
